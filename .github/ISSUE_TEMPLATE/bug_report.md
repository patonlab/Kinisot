---
name: Bug report
about: Kinisot crashed, refused an input, or gave a number you do not believe
labels: bug
---

**Command**

```
kinisot --rct ... --ts ... --iso ...
```

**What happened** (paste the full message or the results block)

**What you expected**

**Files**: attach the output files, or the sections listed in
`docs/file_formats.md` (`NAtoms=`, the `has atomic number ... and mass`
lines, the archive entry). If a file is confidential, say which program
and version produced it and whether the job terminated normally.

**Versions**: `kinisot --version`, `python --version`, operating system.
